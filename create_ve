1. Create virtual environment

conda create --name env_pytorch

2. Install all packages needed, including numpy 1.16.1

conda install -n env_pytorch numpy-1.16.1

* install other packages needed by replacing "numpy-1.16.1" e.g. matplotlib
